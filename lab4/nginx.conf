worker_processes 1;

events { worker_connections 1024; }

http {
   
    sendfile on;
    index index.html;

    server {
        server_name localhost;
        listen 80;
        location / {
            root /usr/share/nginx/html/;
            index index.html;
        }
    }
}
